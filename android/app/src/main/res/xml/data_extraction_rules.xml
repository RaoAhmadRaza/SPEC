<?xml version="1.0" encoding="utf-8"?>
<!--
  SPEC's archive never leaves the device. Cloud backup would upload it to the
  user's Google Drive, which contradicts the promise printed on the welcome
  screen. Device-to-device transfer is excluded for the same reason; the way
  an archive moves to a new phone is the export file the user chooses.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
